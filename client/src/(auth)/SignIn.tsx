import SignInForm from "@/app/features/auth/forms/SignInForm";

const SignIn = () => {
  return (
    <main className="flex flex-1 items-center justify-center">
      <SignInForm />
    </main>
  );
};

export default SignIn;
